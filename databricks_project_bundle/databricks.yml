# This is a Databricks asset bundle definition for databricks_project_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: databricks_project_bundle
  uuid: ca138220-bf61-4941-8404-d9cd69c83aec

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

  warehouse_id:
    description:  WarehouseID

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-39c2cb55-a317.cloud.databricks.com
    variables:
      catalog: youtude_dev
      schema: dev
      warehouse_id: 973a889bc194bb59
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://dbc-39c2cb55-a317.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/likhitha1532@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/likhitha1532@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: youtude_prod
      schema: prod
      warehouse_id: 973a889bc194bb59
    permissions:
      - user_name: likhitha1532@gmail.com
        level: CAN_MANAGE
